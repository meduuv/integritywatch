[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "integritywatch"
version = "0.1.0"
description = "Dependency-free file integrity helpers"
requires-python = ">=3.9"

[tool.setuptools]
py-modules = ["integritywatch"]
